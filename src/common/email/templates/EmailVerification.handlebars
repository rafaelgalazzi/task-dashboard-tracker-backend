<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <!-- Helps clients that respect it (Apple Mail, iOS Mail) -->
    <meta name="color-scheme" content="light dark" />
    <meta name="supported-color-schemes" content="light dark" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <style>
      /* Dark-mode overrides (Apple Mail, iOS Mail, some Gmail surfaces) */
      @media (prefers-color-scheme: dark) {
        .bg       { background: #0b0d10 !important; }
        .card     { background: #14181d !important; border-color: #2a2f36 !important; }
        .text     { color: #e6e8eb !important; }
        .muted    { color: #aab2bd !important; }
        .title    { color: #ffffff !important; }
        .btn      { background: #3b82f6 !important; color: #ffffff !important; }
        .link     { color: #8ab4ff !important; }
      }

      /* Outlook.com / Office 365 web dark mode */
      [data-ogsc] .bg       { background: #0b0d10 !important; }
      [data-ogsc] .card     { background: #14181d !important; border-color: #2a2f36 !important; }
      [data-ogsc] .text     { color: #e6e8eb !important; }
      [data-ogsc] .muted    { color: #aab2bd !important; }
      [data-ogsc] .title    { color: #ffffff !important; }
      [data-ogsc] .btn      { background: #3b82f6 !important; color: #ffffff !important; }
      [data-ogsc] .link     { color: #8ab4ff !important; }
    </style>
  </head>

  <body class="bg" style="margin:0; padding:0; background:#f5f7fb;">
    <!-- Wrapper table for better email client support -->
    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" class="bg" style="background:#f5f7fb;">
      <tr>
        <td align="center" style="padding:24px;">
          <table role="presentation" width="600" cellspacing="0" cellpadding="0" border="0" class="card" style="max-width:600px; width:100%; background:#ffffff; border:1px solid #e6e8eb; border-radius:8px;">
            <tr>
              <td style="padding:24px;">
                <h2 class="title" style="margin:0 0 12px 0; font-family:Arial,Helvetica,sans-serif; font-size:22px; line-height:1.3; color:#1f2937;">
                  Welcome, {{user.name}}!
                </h2>

                <p class="text" style="margin:0 0 16px 0; font-family:Arial,Helvetica,sans-serif; font-size:14px; line-height:1.6; color:#374151;">
                  Please verify your email by clicking:
                  <a href="{{verificationUrl}}" class="btn" style="display:inline-block; margin-left:8px; background:#2563eb; color:#ffffff; text-decoration:none; padding:8px 12px; border-radius:6px;">
                    Verify Email
                  </a>
                </p>

                {{#if expiresAt}}
                  <p class="muted" style="margin:0 0 16px 0; font-family:Arial,Helvetica,sans-serif; font-size:12px; line-height:1.6; color:#6b7280;">
                    This link expires on {{formatDate expiresAt "YYYY-MM-DD HH:mm"}}
                  </p>
                {{/if}}

                <p class="text" style="margin:16px 0 0 0; font-family:Arial,Helvetica,sans-serif; font-size:12px; line-height:1.6; color:#374151;">
                  Having trouble? Open this link in your browser:
                  <a href="{{verificationUrl}}" class="link" style="color:#2563eb; text-decoration:underline;">{{verificationUrl}}</a>
                </p>
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>

  </body>
</html>
